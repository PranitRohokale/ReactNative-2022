/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import ProfilePic from './components/profilePic';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ProfilePic);
